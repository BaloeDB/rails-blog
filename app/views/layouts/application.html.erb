<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if !@current_user.nil? %>
      <nav class="bg-gray-200 p-4 text-black">
        <div class="container mx-auto flex justify-between items-center">
          <div>
            <h1 class="text-2xl font-bold">Welcome to the Blog, <%= @current_user.login %>!</h1>
          </div>
          <div>
            <%= link_to "Logout", user_session_path(@current_user_session, method: :destroy), class:"bg-blue-300 hover:bg-blue-500 text-black font-bold py-2 px-4 rounded" %>
          </div>
        </div>
      </nav>
    <% end %>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
