<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav>
      <% if !@current_user.nil? %>
        <h1>Welcome to the blog, <%= @current_user.login %>!</h1>
        <%= link_to "Link: Logout",  user_session_path(@current_user_session, method: :destroy) %>
        <br /><br>
      <% end %>
    </nav>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
